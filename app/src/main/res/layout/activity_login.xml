<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="24dp"
    tools:context=".Login">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="32dp"
        android:text="เข้าสู่ระบบ"
        android:textSize="32sp"
        android:textStyle="bold" />

    <!-- ช่องกรอกรหัสนักศึกษา (ID ที่ถูกต้องคือ editUser) -->
    <EditText
        android:id="@+id/editUser"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:hint="รหัสนักศึกษา (User)"
        android:inputType="text" />

    <!-- ช่องกรอกรหัสผ่าน (ID ที่ถูกต้องคือ editPassword) -->
    <EditText
        android:id="@+id/editPassword"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="24dp"
        android:hint="รหัสผ่าน (Password)"
        android:inputType="textPassword" />

    <!-- ปุ่มเข้าสู่ระบบ (ID ที่ถูกต้องคือ buttonLogin) -->
    <Button
        android:id="@+id/buttonLogin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="GO" />

    <!-- ปุ่มไปหน้าลงทะเบียน (ID ที่ถูกต้องคือ buttonRegister) -->
    <Button
        android:id="@+id/Register"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="ยังไม่มีบัญชี? ลงทะเบียนที่นี่"
        style="@style/Widget.MaterialComponents.Button.TextButton"/>
    <!-- <<<<< เพิ่มส่วนนี้เข้ามาใหม่ >>>>> -->
    <!-- ProgressBar (วงกลมหมุนๆ) ที่จะแสดงตอนกำลังโหลด -->
    <ProgressBar
        android:id="@+id/loadingProgressBar"
        style="?android:attr/progressBarStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone"
        tools:visibility="visible" />
    <!-- android:visibility="gone" คือการซ่อนมันไว้ก่อนในตอนแรก -->
</LinearLayout>
